# 🏗️ GhostBridge Architecture

> **Version:** 0.1.0 | **Last Updated:** 2025-12-18

---

## 📋 Overview

GhostBridge is a transparent network implant designed for long-term persistence in Red Team operations. This document describes the technical architecture.

---

## 🎯 Design Goals

| Goal | Priority | Description |
|------|----------|-------------|
| **Stealth** | P0 | Invisible to network scans and monitoring |
| **Persistence** | P0 | Survives reboots, reconnects automatically |
| **Reliability** | P1 | 99.9%+ uptime, multiple fallback options |
| **Integration** | P1 | Seamless MoMo C2 integration |
| **Simplicity** | P2 | Easy deployment, minimal configuration |

---

## 🔧 System Architecture

### High-Level Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           GHOSTBRIDGE DEVICE                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                        USER SPACE                                      │ │
│  │                                                                        │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │ │
│  │  │ Beacon   │  │ Command  │  │ Tunnel   │  │ Stealth  │  │ MoMo     │ │ │
│  │  │ Service  │  │ Handler  │  │ Manager  │  │ Module   │  │ Client   │ │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘ │ │
│  │       │             │             │             │             │        │ │
│  │       └─────────────┴──────┬──────┴─────────────┴─────────────┘        │ │
│  │                            │                                           │ │
│  │                     ┌──────▼──────┐                                    │ │
│  │                     │   Core      │                                    │ │
│  │                     │   Engine    │                                    │ │
│  │                     └──────┬──────┘                                    │ │
│  │                            │                                           │ │
│  └────────────────────────────┼───────────────────────────────────────────┘ │
│                               │                                              │
│  ┌────────────────────────────┼───────────────────────────────────────────┐ │
│  │                     KERNEL SPACE                                       │ │
│  │                            │                                           │ │
│  │  ┌──────────┐       ┌──────▼──────┐       ┌──────────┐                │ │
│  │  │ WireGuard│◄─────►│   Linux     │◄─────►│ Netfilter│                │ │
│  │  │ Module   │       │   Bridge    │       │ (iptables)│               │ │
│  │  └────┬─────┘       └──────┬──────┘       └──────────┘                │ │
│  │       │                    │                                           │ │
│  └───────┼────────────────────┼───────────────────────────────────────────┘ │
│          │                    │                                              │
├──────────┼────────────────────┼──────────────────────────────────────────────┤
│          │                    │                                              │
│    ┌─────▼─────┐        ┌─────▼─────┐                                       │
│    │   wg0     │        │    br0    │                                       │
│    │ (tunnel)  │        │ (bridge)  │                                       │
│    └───────────┘        └─────┬─────┘                                       │
│                               │                                              │
│                    ┌──────────┴──────────┐                                  │
│                    │                     │                                  │
│              ┌─────▼─────┐         ┌─────▼─────┐                           │
│              │   eth0    │         │   eth1    │                           │
│              │   (WAN)   │         │   (LAN)   │                           │
│              └─────┬─────┘         └─────┬─────┘                           │
│                    │                     │                                  │
└────────────────────┼─────────────────────┼──────────────────────────────────┘
                     │                     │
                     ▼                     ▼
              ┌───────────┐         ┌───────────┐
              │   WALL    │         │  TARGET   │
              │   PORT    │         │  DEVICE   │
              └───────────┘         └───────────┘
```

---

## 🌉 Bridge Architecture

### Layer 2 Transparent Bridge

```
Wall Port (eth0)                    Target Device (eth1)
      │                                    │
      │  ┌─────────────────────────────┐   │
      │  │         BRIDGE (br0)        │   │
      │  │                             │   │
      ├──►  [eth0] ◄──► [eth1]  ◄──────┤
      │  │                             │   │
      │  │  • No IP address            │   │
      │  │  • MAC learning enabled     │   │
      │  │  • STP disabled             │   │
      │  │  • Promiscuous mode         │   │
      │  │                             │   │
      │  └─────────────────────────────┘   │
      │                                    │
      ▼                                    ▼
   Frames pass through transparently
```

### MAC Cloning Strategy

```
Before GhostBridge:
  Wall ────── Target PC (MAC: AA:BB:CC:DD:EE:FF)
  
After GhostBridge:
  Wall ────── GhostBridge ────── Target PC
              │
              └── eth0 MAC: AA:BB:CC:DD:EE:FF (cloned)
                  eth1 MAC: Original
                  
Network sees: Same MAC on wall port = No change detected
```

### Bridge Configuration

```bash
# Create bridge
ip link add name br0 type bridge
ip link set br0 up

# Disable STP (faster convergence)
echo 0 > /sys/class/net/br0/bridge/stp_state

# Add interfaces
ip link set eth0 master br0
ip link set eth1 master br0

# Enable promiscuous mode
ip link set eth0 promisc on
ip link set eth1 promisc on

# Clone MAC from target (read from eth1)
TARGET_MAC=$(cat /sys/class/net/eth1/address)
ip link set eth0 address $TARGET_MAC
```

---

## 🔐 Tunnel Architecture

### WireGuard Tunnel

```
┌─────────────────────────────────────────────────────────────────┐
│                       GHOSTBRIDGE                                │
│                                                                  │
│  ┌────────────┐                              ┌────────────┐     │
│  │    br0     │                              │    wg0     │     │
│  │  (bridge)  │                              │  (tunnel)  │     │
│  │            │                              │            │     │
│  │  No IP     │                              │ 10.66.66.2 │     │
│  └────────────┘                              └─────┬──────┘     │
│                                                    │            │
│                                              ┌─────▼──────┐     │
│                                              │ WireGuard  │     │
│                                              │ Encryption │     │
│                                              └─────┬──────┘     │
│                                                    │            │
│  ┌─────────────────────────────────────────────────▼──────────┐ │
│  │                    Network Stack                           │ │
│  │                                                            │ │
│  │   Bridge traffic flows through WITHOUT touching tunnel     │ │
│  │   Tunnel traffic originates FROM device (beacon, C2)      │ │
│  │                                                            │ │
│  └─────────────────────────────────────────────────┬──────────┘ │
│                                                    │            │
└────────────────────────────────────────────────────┼────────────┘
                                                     │
                                                     ▼
                                              ┌────────────┐
                                              │  Internet  │
                                              │  (via br0) │
                                              └─────┬──────┘
                                                    │
                                                    ▼
                                              ┌────────────┐
                                              │ C2 Server  │
                                              │ 10.66.66.1 │
                                              └────────────┘
```

### Tunnel Modes

| Mode | Port | Use Case |
|------|------|----------|
| WireGuard UDP | 51820 | Primary - fastest |
| WireGuard TCP | 443 | Firewalled networks |
| wstunnel | 443 | WebSocket wrapper |
| Cloudflare Tunnel | 443 | Zero-trust networks |
| iodine | 53 | DNS-only networks |

### Fallback Chain

```
1. WireGuard UDP 51820
   │
   ├── Fail after 30s
   ▼
2. WireGuard TCP 443
   │
   ├── Fail after 30s
   ▼
3. wstunnel over HTTPS
   │
   ├── Fail after 30s
   ▼
4. Cloudflare Tunnel
   │
   ├── Fail after 60s
   ▼
5. DNS Tunnel (iodine)
   │
   ├── Fail → wait 5 min
   ▼
6. Retry from step 1
```

---

## 📡 Beacon Architecture

### Beacon Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                       BEACON SERVICE                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐                │
│  │  Timer   │────►│ Collect  │────►│  Send    │                │
│  │ (5 min)  │     │  Status  │     │  Beacon  │                │
│  └──────────┘     └──────────┘     └────┬─────┘                │
│                                          │                      │
│  ┌──────────┐     ┌──────────┐          │                      │
│  │  Jitter  │     │  Queue   │◄─────────┘                      │
│  │ (±1 min) │     │ Commands │                                 │
│  └──────────┘     └────┬─────┘                                 │
│                        │                                        │
│                        ▼                                        │
│                 ┌──────────┐                                   │
│                 │ Execute  │                                   │
│                 │ Commands │                                   │
│                 └──────────┘                                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Beacon Protocol

```
DEVICE                                          C2 SERVER
   │                                                │
   │  POST /api/ghostbridge/beacon                 │
   │  {device_id, status, uptime, ...}             │
   │ ─────────────────────────────────────────────►│
   │                                                │
   │  200 OK                                       │
   │  {commands: [{id, action, payload}]}          │
   │ ◄─────────────────────────────────────────────│
   │                                                │
   │  [Execute command locally]                    │
   │                                                │
   │  POST /api/ghostbridge/response               │
   │  {command_id, result, status}                 │
   │ ─────────────────────────────────────────────►│
   │                                                │
   │  200 OK                                       │
   │ ◄─────────────────────────────────────────────│
   │                                                │
```

---

## 🛡️ Stealth Architecture

### Stealth Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      STEALTH MODULE                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Layer 1: Network Stealth                                       │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • No IP on bridge interfaces                                ││
│  │ • MAC cloning from target                                   ││
│  │ • No ARP replies for bridge                                 ││
│  │ • No listening ports                                        ││
│  │ • Outbound-only connections                                 ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Layer 2: Traffic Stealth                                       │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • Timing jitter on beacons                                  ││
│  │ • Traffic blending with normal patterns                     ││
│  │ • Encrypted tunnel (WireGuard)                              ││
│  │ • DNS over HTTPS for lookups                                ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Layer 3: Host Stealth                                          │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • RAM-only logs (tmpfs)                                     ││
│  │ • Encrypted filesystem                                      ││
│  │ • Minimal running processes                                 ││
│  │ • Fake identity if probed                                   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Layer 4: Anti-Forensics                                        │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • Secure erase on panic                                     ││
│  │ • No persistent logs                                        ││
│  │ • Key in volatile memory                                    ││
│  │ • Self-destruct capability                                  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Panic Sequence

```
PANIC TRIGGER
     │
     ▼
┌─────────────┐
│ Stop all    │
│ services    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Wipe RAM    │
│ (keys, logs)│
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Secure erase│
│ config files│
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Overwrite   │
│ with zeros  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Halt system │
│ or reboot   │
└─────────────┘
```

---

## 📁 Directory Structure

```
/
├── etc/
│   └── ghostbridge/
│       ├── config.yml           # Main configuration
│       ├── device.key           # Device identity key
│       └── wireguard/
│           ├── private.key      # WireGuard private key
│           └── wg0.conf         # WireGuard config
├── opt/
│   └── ghostbridge/
│       ├── bin/                 # Executables
│       │   ├── ghostbridge      # Main binary
│       │   └── gb-cli           # CLI tool
│       ├── lib/                 # Python modules
│       │   └── ghostbridge/
│       │       ├── core/
│       │       ├── infrastructure/
│       │       └── c2/
│       └── scripts/             # Helper scripts
│           ├── setup-bridge.sh
│           └── panic.sh
├── var/
│   └── ghostbridge/             # Runtime data (tmpfs)
│       ├── run/                 # PID files
│       └── log/                 # Logs (RAM only)
└── lib/
    └── systemd/
        └── system/
            ├── ghostbridge.service
            ├── ghostbridge-tunnel.service
            └── ghostbridge-beacon.service
```

---

## 🔌 Hardware Pinout (NanoPi R2S)

```
┌─────────────────────────────────────────┐
│              NanoPi R2S                  │
│                                          │
│  ┌────────┐              ┌────────┐     │
│  │  WAN   │              │  LAN   │     │
│  │ (eth0) │              │ (eth1) │     │
│  │  RJ45  │              │  RJ45  │     │
│  └────────┘              └────────┘     │
│                                          │
│  ┌────────┐  ┌────────┐  ┌────────┐     │
│  │ USB-C  │  │ USB-A  │  │ MicroSD│     │
│  │ Power  │  │  Port  │  │ Slot   │     │
│  └────────┘  └────────┘  └────────┘     │
│                                          │
│  LED1 (Power) ● ● LED2 (Status)         │
│                                          │
└─────────────────────────────────────────┘

Connections:
  eth0 (WAN) ──► Wall port / Switch
  eth1 (LAN) ──► Target device
  USB-C      ──► Power (5V from target or wall)
  USB-A      ──► Optional: WiFi dongle, storage
  
LED Meanings:
  LED1 (Green) : Power on
  LED2 (Blue)  : Tunnel status
    - Solid    : Connected
    - Blinking : Connecting
    - Off      : Disconnected
```

---

## 📊 Data Flow Diagrams

### Normal Traffic Flow (Bridged)

```
Target PC                  GhostBridge                    Internet
    │                          │                              │
    │  HTTP Request            │                              │
    │ ─────────────────────────┼──────────────────────────────►
    │                          │                              │
    │  (Passes through br0     │                              │
    │   without inspection)    │                              │
    │                          │                              │
    │  HTTP Response           │                              │
    │ ◄────────────────────────┼──────────────────────────────
    │                          │                              │
```

### C2 Traffic Flow (Tunneled)

```
GhostBridge                    Internet                   C2 Server
    │                              │                          │
    │  WireGuard Handshake         │                          │
    │ ─────────────────────────────┼─────────────────────────►│
    │                              │                          │
    │  Encrypted Tunnel            │                          │
    │ ◄────────────────────────────┼─────────────────────────►│
    │                              │                          │
    │  Beacon (encrypted)          │                          │
    │ ═════════════════════════════╪═════════════════════════►│
    │                              │                          │
    │  Commands (encrypted)        │                          │
    │ ◄════════════════════════════╪═════════════════════════│
    │                              │                          │
```

---

## 🧪 Testing Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       TEST ENVIRONMENT                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐      ┌──────────────────┐                │
│  │   Virtual Net    │      │   Real Hardware  │                │
│  │   (VMs + veth)   │      │   (NanoPi R2S)   │                │
│  └────────┬─────────┘      └────────┬─────────┘                │
│           │                         │                           │
│           └────────────┬────────────┘                           │
│                        │                                        │
│                 ┌──────▼──────┐                                │
│                 │   pytest    │                                │
│                 │   Suite     │                                │
│                 └──────┬──────┘                                │
│                        │                                        │
│  ┌─────────────────────┼─────────────────────┐                 │
│  │                     │                     │                 │
│  ▼                     ▼                     ▼                 │
│ Unit               Integration           Stealth              │
│ Tests              Tests                 Tests                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

*GhostBridge Architecture v0.1.0*

